# Assets

Store game-owned source assets here, such as CHR source data, palettes, maps, music, sound data, and conversion inputs.

The current ROM keeps its tile graphics in `src/main.asm`, so this folder is intentionally minimal.
