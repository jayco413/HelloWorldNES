MEMORY {
    PRG: start = $C000, size = $4000, file = %O, fill = yes;
}

SEGMENTS {
    CODE:    load = PRG, type = ro, start = $C000;
    RODATA:  load = PRG, type = ro;
    VECTORS: load = PRG, type = ro, start = $FFFA;
}
